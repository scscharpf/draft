from setuptools import setup

setup(name='get_object',
      version='0.1',
      packages=['src',
                'src.getObject',
                'src.restHandlers',
                'src.awsBase']
      )

