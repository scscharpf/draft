from setuptools import setup

setup(name='list_objects',
      version='0.1',
      packages=['src',
                'src.listObject',
                'src.restHandlers',
                'src.awsBase']
      )
